<%= form_for @creator, :html => { :class => "form-horizontal creator" } do |f| %>

  <% if @creator.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@creator.errors.count, "error") %> prohibited this creator from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @creator.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :user_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:user_id) %>
  </div>
  <div class="form-group">
    <%= f.label :skill, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :skill, :class => 'form-control' %>
    </div>
    <%=f.error_span(:skill) %>
  </div>
  <div class="form-group">
    <%= f.label :body, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :body, :class => 'form-control' %>
    </div>
    <%=f.error_span(:body) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                creators_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
