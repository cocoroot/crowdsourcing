<%= form_for(@creator_product) do |f| %>
  <% if @creator_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creator_product.errors.count, "error") %> prohibited this creator_product from being saved:</h2>

      <ul>
      <% @creator_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :creator_id %><br>
    <%= f.number_field :creator_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
