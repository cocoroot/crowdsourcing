<%= form_for(@product_point) do |f| %>
  <% if @product_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_point.errors.count, "error") %> prohibited this product_point from being saved:</h2>

      <ul>
      <% @product_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
